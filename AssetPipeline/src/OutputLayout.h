#pragma once

inline const std::filesystem::path kModelFolder = "Models";
inline const std::filesystem::path kModelTextureFolder = kModelFolder / "Textures";
inline const std::filesystem::path kAnimationFolder = kModelFolder / "Animations";   // a folder per model, named after it
inline const std::filesystem::path kTextureFolder = "Textures";   // textures no model uses